[user]
        email = XXX
        name = XXX
[alias]
        co = checkout
        br = branch
        ci = commit
        st = status
        sm = submodule
[credential]
        helper = store
[core]
        autocrlf = input
        safecrlf = warn
        quotepath = off
[filter "lfs"]
        clean = git-lfs clean -- %f
        smudge = git-lfs smudge -- %f
        process = git-lfs filter-process
        required = true
